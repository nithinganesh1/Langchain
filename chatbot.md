# Create a New Chatbot usign openapi api key and langchain

# firt crate a langchain accout and crate api key
## take any llm model api key (i used open router for that)
### created .env file with langchain, openapi keys and langchain project name

## calling api is very is task
## for we have so many module in langchain, we need to how to use thease modules for diffrent diffrent calls algong with what all dependcies we spesifically

## ChatOpenAI for connecting openai
## ChatPromptTemplate: basicly we givign initial prompt template
## StrOutputParsers: default, we can make custome also (how output shoud come)
## langchain provides features taht can attach in the form of chains eg, chat prompt template --> llm --> output pareser

